{ pkgs, ... }: {
  wrapperFeatures = { };
  extraSessionCommands = "";
  extraOptions = [ ];
  config = {
    fonts = { };
    window = { commands = [ ]; };
    floating = { criteria = [ ]; };
    focus = { };
    assigns = { };
    keycodebindings = { };
    colors = { };
    bars = [ ];
    gaps = { };
    modes = { };
    keybindings = { };
    input = { };
    output = { };
    startup = [ ];
  };
  extraConfig = "";
}
